{ user-pkgs, ... }:
{
	# programs.firefox = {
	# 	enable = true;
	# 	package = user-pkgs.firefox;
	# };

	# hm = {
	# 	wayland.windowManager.hyprland = {
	# 		extraConfig = ''
	# 			bind = SUPER, B, exec, ${user-pkgs.firefox;}/bin/firefox
	# 		'';
	# 	};
	# };
}
